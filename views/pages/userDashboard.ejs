<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chinmudra Academy for Performing Arts</title>
    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-red: #B31312;
            --white: #ffffff;
        }
        
        body {
            font-family: 'Roboto', sans-serif;
        }
        
        .heading {
            font-family: 'Playfair Display', serif;
        }
        
        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(179, 19, 18, 0.2);
        }

        .mandala-bg {
            background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="45" fill="none" stroke="%23B31312" stroke-width="0.5" opacity="0.1"/></svg>');
            background-size: 200px;
        }

        #loginForm, #signupForm {
            display: none;
        }

        /* #dashboard, #batchSelection, #paymentHistory {
            display: none;
        } */

        .active {
            display: block !important;
        }
       
    </style>
</head>
<body class="bg-white mandala-bg min-h-screen">
    <%- include('../partials/navbar') %>
     <!-- Dashboard -->
     <div id="dashboard">
        <div class="container mx-auto px-4 py-8">
            <h2 class="heading text-3xl font-bold text-[#B31312] mb-8">Welcome, <span id="userName">User</span>!</h2>
            <div class="grid md:grid-cols-2 gap-6">
                <div class="bg-white p-6 rounded-lg shadow-md border border-[#B31312] card-hover cursor-pointer" onclick="window.location.href='/chinmudra/batches'">
                    <i class="bi bi-calendar-check text-4xl text-[#B31312]"></i>
                    <h3 class="text-xl font-bold text-[#B31312] mt-4">Batch Selection</h3>
                    <p class="text-gray-600">Choose your preferred batch and make payment</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-md border border-[#B31312] card-hover cursor-pointer" onclick="window.location.href='/chinmudra/payments/history/<%=userId%>'">
                    <i class="bi bi-receipt text-4xl text-[#B31312]"></i>
                    <h3 class="text-xl font-bold text-[#B31312] mt-4">Payment History</h3>
                    <p class="text-gray-600">View your payment records</p>
                </div>
            </div>
        </div>
    </div>
    
</body>
</html>