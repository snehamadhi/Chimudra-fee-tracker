<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login | Chinmudra Academy</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-red: #B31312;
            --white: #ffffff;
        }
        body {
            font-family: 'Roboto', sans-serif;
        }
        .heading {
            font-family: 'Playfair Display', serif;
        }
        .mandala-bg {
            background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="45" fill="none" stroke="%23B31312" stroke-width="0.5" opacity="0.1"/></svg>');
            background-size: 200px;
        }
    </style>
</head>

<body class="bg-white mandala-bg min-h-screen">
    <%- include('../partials/navbar') %>

    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-white p-8 rounded-lg shadow-lg border-2 border-[#B31312] w-full max-w-md">
            <h2 class="heading text-3xl font-bold text-center text-[#B31312] mb-8">
                Admin Login – Chinmudra Academy
            </h2>

            <form action="/chimudra/admin/login" method="post">
                <div class="mb-6">
                    <input type="email" name="email" placeholder="Email"
                        class="w-full px-4 py-2 rounded border border-[#B31312] focus:outline-none focus:ring-2 focus:ring-[#B31312]"
                        required>
                </div>

                <div class="mb-6">
                    <input type="password" name="password" placeholder="Password"
                        class="w-full px-4 py-2 rounded border border-[#B31312] focus:outline-none focus:ring-2 focus:ring-[#B31312]"
                        required>
                </div>

                <button type="submit"
                    class="w-full bg-[#B31312] text-white py-2 rounded-lg hover:bg-red-700 transition duration-300">
                    Login
                </button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        const urlParams = new URLSearchParams(window.location.search);
        const error = urlParams.get('error');

        if (error) {
            Swal.fire({
                title: 'You are not an Admin',
                text: error,
                confirmButtonColor: '#B31312'
            });
        }
    </script>
</body>
</html>
